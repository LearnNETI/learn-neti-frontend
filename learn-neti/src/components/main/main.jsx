import React from "react";
import Feed from "../feed/feed";
const Main = (props) => {
    return (
        <main className="main-container">
            <div className="wrapper">
            <Feed></Feed>
            </div>
            
        </main>
    );
};

export default Main;